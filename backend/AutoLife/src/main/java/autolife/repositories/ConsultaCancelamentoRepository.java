package autolife.repositories;

import autolife.entities.ConsultaCancelamento;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ConsultaCancelamentoRepository extends JpaRepository<ConsultaCancelamento, Long> {
}